// 2.1 Variable Names

#include <stdio.h>

int main() {
    // variable names
    int age = 25;
    int _count = 10; // not recommended to start with underscore
                     // since library routines use such names
    int total_sum = 100;
    int value2 = 50;

    // case-sensitive
    int data = 1;
    int Data = 2;
} 
